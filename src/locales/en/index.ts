import texts from './translation.json';
import errorMessages from './errors.json';
import recommendedBar from './recommendedbar.json';

const en = Object.assign(texts, errorMessages, recommendedBar);

export default en;
